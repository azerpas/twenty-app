/**
 * All the variations of view styling within the app.
 *
 * You want to customize these to whatever you need in your app.
 */
export const presets = {
  flex: 1
}

/**
 * A list of preset names.
 */
export type ViewPresets = keyof typeof presets